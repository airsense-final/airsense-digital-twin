version: '3.8'

services:
  twin-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: airsense-twin-backend
    restart: unless-stopped
    ports:
      - "10002:10002"
    env_file:
      - .env
    volumes:
      - ./backend/logs:/app/logs
